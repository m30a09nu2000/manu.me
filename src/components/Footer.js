import { Box, Typography } from "@mui/material";
import React from "react";

const Footer = () => {
  return (
    <Box
      sx={{
        textAlign: "center",

        marginTop: "-6px",
      }}
    >
      <Typography variant="caption">2024-PRESENT @ Manu</Typography>
    </Box>
  );
};

export default Footer;
